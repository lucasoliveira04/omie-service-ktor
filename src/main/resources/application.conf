ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.omie.ApplicationKt.module ]
    }
}
ktor {
  rabbitmq {
    uri = "amqp://guest:guest@localhost:5672"
    defaultConnectionName = "default_connection"
    dispatcherThreadPollSize = 4
    connectionAttempts = 10
    attemptDelay = 5
    tls {
      enabled = false
    }
  }
}